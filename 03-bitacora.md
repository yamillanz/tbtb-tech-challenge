# 03 — Bitácora

## Matriz de trazabilidad

| Criterio | Implementación (archivos / commits) | Prueba que lo verifica | Estado |
|---|---|---|---|
| CA-2 — Registro de contactos | `scripts/001_schema.sql` · `api/src/Services/ContactService.cs` · `api/src/Controllers/ContactsController.cs` · formulario en `web/src/app/contact-form/` | `RegistrarContacto_CuandoElGestorCompletaElFormulario_ElContactoQuedaAsociadoAlPaciente` (xUnit contra SQL Server) | Cubierto |
| CA-3 — Corrección con trazabilidad | _pendiente: se completa al implementar_ | `CorregirContacto_CuandoSeCorrigeUnContacto_ElOriginalNoCambiaYElReporteMuestraElValorVigente` | Parcial |
| CA-4 — Vista del mes con filtros | _pendiente: se completa al implementar_ | `ConsultarContactos_CuandoSeFiltraPorGestorYCiudad_SoloSeMuestranLosQueCumplenAmbosFiltros` | Parcial |

Los estados válidos son: cubierto, parcial o fuera de alcance. Un criterio declarado en alcance sin prueba que lo respalde cuenta como no cubierto. La columna se actualiza al cerrar cada tarea.

## Registro de decisiones y uso de IA

Lista cronológica. Cada entrada: qué se decidió, por qué, y si la propuesta salió del asistente o de mí.

| Fecha/hora | Decisión | Motivo | Origen |
|---|---|---|---|
| 17/09 | Alcance cerrado en CA-2 + CA-3 + CA-4 | Forman una historia vertical completa (registrar → corregir → consultar) y concentran el criterio de entorno regulado en CA-3. Se descartó CA-1 por no aportar señal técnica nueva. | Propia, contrastada con el asistente |
| 17/09 | CA-5 y CA-6 excluidos y declarados | Sus reglas no están definidas en el extracto (no son implementables sin inventar negocio). Se declaran como exclusión justificada, no como oferta condicionada. | Propia |
| 17/09 | Consultas enviadas al coordinador del proceso (3 bloqueantes + recortes declarados) | El enunciado invita a preguntar y los huecos son intencionales; se escalan solo los bloqueantes, cada uno con su supuesto, para no detener el trabajo si no hay respuesta. | Propia |
| 17/09 | El coordinador respondió: no resuelve las consultas por correo (está previsto así), confirma los supuestos como camino y remite las exclusiones al plan | Refuerza que lo evaluado es la solidez del supuesto, su impacto declarado y la coherencia entre modelo, contrato y pruebas; se sigue con los supuestos documentados. | Externa (respuesta del coordinador del proceso) |
| 17/09 | Corrección modelada como enmienda inmutable (no actualización in-place) | En un entorno regulado un registro no se sobrescribe sin rastro; el contacto original queda intacto y cada corrección agrega motivo, autor y fecha. | Propia |
| 17/09 | SQL Server y API en contenedores Docker; acceso a datos con EF Core | El entorno en contenedor hace el README reproducible en una máquina limpia; EF Core es el ORM estándar de ASP.NET Core. | Propia, con sugerencia del asistente |
| 17/09 | Esquema con scripts `.sql` numerados escritos a mano (`scripts/001_schema.sql`, `002_seed.sql`); EF Core solo mapea las tablas existentes | El enunciado pide el esquema versionado en `scripts/` y exige poder sostener cada línea en la defensa: un esquema propio se explica completo; el código autogenerado de las migraciones EF, no. | Asistente (aceptada tras comparar con migraciones EF) |
| 17/09 | Contrato completado con `GET /api/patients` y `GET /api/gestors` | El formulario de registro y el filtro por gestor necesitan datos de referencia que el contrato no devolvía; el filtro de ciudad se deriva de los pacientes cargados para no multiplicar endpoints. | Asistente (detectó el hueco en la revisión del plan, previa al commit) |
| 17/09 | "El reporte" de CA-3 interpretado como el registro de contactos del mes en curso (la vista del mes), no como el reporte de adherencia | CA-6 está fuera de alcance; sin esta interpretación, CA-3 quedaría sin dónde verificarse en la entrega. | Propia, a partir de un hueco señalado por el asistente |
| 17/09 | Supuesto de "paciente activo" y unicidad compuesta del documento incorporados al modelo (`Status` + `StatusChangedAt`, único sobre tipo + número) | El modelo tenía un campo de estado sin supuesto que lo respaldara y un unique solo sobre el número de documento, que rechazaría pacientes legítimos con documentos de distinto tipo. | Asistente (detectado en la revisión del plan) |
| 17/09 | Trabajo organizado como cambios de OpenSpec (propose → apply → archive) | Es la metodología con la que ya trabajo: cada criterio de aceptación se convierte en un cambio con propuesta, tareas y deltas de especificación, y al cerrarse queda archivado con las specs actualizadas. Aporta trazabilidad de extremo a extremo, que es exactamente lo que este ejercicio pide demostrar. | Propia |
| 17/09 | Rechazada una propuesta del asistente: crear `openspec/project.md` antes del primer cambio de OpenSpec | El candidato cuestionó la fuente; la verificación contra la documentación oficial mostró que `project.md` es del esquema legacy y que la versión actual usa `config.yaml` con sección `context:` (opcional). Se corrigió antes de crear el archivo. | Asistente (propuesta corregida a partir del cuestionamiento del candidato) |
| 17/09 | La ejecución de OpenSpec se lleva comando a comando, manualmente (propose → apply → archive, un cambio por vez) | Control directo del flujo por el candidato: cada propuesta se revisa antes de avanzar y cada artefacto queda defendible. | Propia |
| 17/09 | Regla de trabajo fijada: el asistente hace commit y push solo cuando el candidato lo indica, nunca por iniciativa propia | El historial es parte de la entrega y su contenido y momento son decisiones del autor; la regla queda también registrada en `AGENTS.md`. | Propia |
| 17/09 | Aclarada la correspondencia entre la secuencia de trabajo (plan §5) y los cambios de OpenSpec (§8): las tareas 3 a 7 se ejecutan dentro de cada cambio, en vertical; el scaffolding (tarea 2) precede a todos los cambios | La frase "cada bloque de esta secuencia pertenece a un cambio" era imprecisa: las tareas 1 y 2 no pertenecen a ningún cambio. Se precisa la redacción sin tocar alcance ni decisiones. | Propia (surgida al iniciar la ejecución del plan) |
| 17/09 | Scaffolding ejecutado como tarea 2 del plan: API en un solo proyecto (capas por carpetas y namespaces, sin multi-proyecto), Angular 20.3 (el enunciado exige 17 o superior), `docker-compose.yml` del repo con contraseña por variable de entorno y `.env.example` | Estructura simple y explicable en la defensa, preferida a una solución multi-proyecto; el scaffolding no contiene lógica de negocio, que llega con cada cambio de OpenSpec. | Asistente (aceptada) |
| 17/09 | Eliminado Swagger/Swashbuckle del API, que el scaffolding mantenía como parte de la plantilla | Fuera de alcance para esta entrega y excesivo para lo que se pide: el contrato del plan (§4) documenta la interfaz y las pruebas atestiguan el comportamiento. Corrige la aceptación inicial de la plantilla estándar del asistente. | Propia (rechazo de lo mantenido por el asistente de la plantilla) |
| 17/09 | Linters configurados en ambos lados: API con `.editorconfig` + reglas Roslyn + `dotnet format` (opción nativa del SDK, sin paquetes extra); `web/` con ESLint y angular-eslint (v20, en lockstep con Angular 20); sin Prettier | El enunciado pide formato consistente con linter si es posible; la vía nativa del SDK evita dependencias adicionales (mismo criterio que la eliminación de Swagger) y ESLint es el estándar del ecosistema Angular. | Asistente (aceptada; el candidato recortó Prettier) |
| 17/09 | Rechazado el Spanglish del código generado por el asistente (acciones `Registrar`, `RegistrarContactoAsync`, variables `contacto`, señales `pacientes`, claves CSS `error-campo`, entre otros): todo identificador va en inglés; el español queda solo en los datos (catálogos, seed), la interfaz y los nombres de las pruebas (espejo verbatim de los criterios del PRD) | Mezclar idiomas en los identificadores no es estándar de la industria y dificulta la lectura; "Gestor" se conserva como término de dominio que el PRD y el contrato del plan (§4) ya usan. | Propia (el candidato rechaza y corrige lo generado) |
| 17/09 | Pruebas de interfaz comportamentales del formulario con Testing Library (change `add-contact-form-ui-tests`): se descartan las pruebas triviales de creación y se agregan al spec los requerimientos observables de la pantalla | El enunciado declara que las pruebas de interfaz suman; el criterio del candidato es renderizar, interactuar y observar el comportamiento, no verificar que el componente "se creó". | Propia (decisión del candidato, herramienta evaluada por el asistente) |
| 17/09 | Correcciones halladas por la suite comportamental: el prefill de fecha usa la hora del programa (UTC-5, como el backend) y los errores del servidor son visibles junto al campo aunque no esté tocado | El prefill en UTC puro cruzaba al día siguiente después de las 19:00 local y fallaría el último día del mes; los errores del servidor quedaban ocultos si el campo no había sido tocado, un caso que la verificación manual con navegador no cubrió. | Asistente (detectadas por las pruebas comportamentales y corregidas) |
| 17/09 | El requisito de datos de prueba se formalizó como capability `seed-data` con sus escenarios (re-ejecución sin duplicados, fechas del mes en curso sin literales) | El CLI de OpenSpec v1.3 exige al menos un delta por change y el plan §8 preveía "tasks sin delta" para este cambio de infraestructura; convertir el requisito mínimo 5 del enunciado en requerimiento del spec lo vuelve trazable en la fuente de verdad. | Asistente (aceptada tras verificar la restricción del validador) |
| 17/09 | Seed `002_seed.sql` con fechas relativas al mes en curso; defecto de la primera guarda idempotente hallado al re-ejecutar y corregido (todas las inserciones envueltas en un solo batch) | El `RETURN` en un batch aparte no frenaba el batch de inserciones: la re-ejecución duplicó gestores y contactos. El escenario "re-ejecución sin duplicados" del spec lo destapó; la corrección quedó verificada con los conteos estables (6/16/30). | Asistente (defecto hallado por la verificación del escenario del spec y corregido) |
| 17/09 | Recidiva del Spanglish detectada por el candidato en el bloque de corrección (`abrirCorreccion`, `guardar`, `cargarContactos`, `aplicarErroresDelServidor`, `vigente`, `CrearArregloAsync`, tupla `Paciente/Contacto`) y eliminada: todo identificador vuelto a inglés | La regla ya estaba fijada en la bitácora y el asistente la reincidió en el bloque de CA-3; la revisión del código por el autor la capturó. El español queda solo en datos, interfaz y nombres de prueba. | Propia (el candidato detecta y corrige la recidiva del asistente) |
| 17/09 | `applyServerErrors` del listado refactorizado con retorno temprano (guarda invertida sobre `errors`) | Queda el flujo plano sin doble anidación; la suite comportamental confirma el comportamiento sin cambios. | Propia (petición del candidato al revisar el código) |
| 17/09 | `CreateAmendmentAsync` recompuesto: la validación y aplicación de los campos pedidos se aisló en el método interno `ApplyRequestedChanges` | El método principal queda como flujo (motivo → carga → aplicar → enmienda → guardar) y las verificaciones por campo viven en un lugar con nombre explícito; las pruebas xUnit confirman el comportamiento. | Propia (petición del candidato al revisar el código) |
| 17/09 | UX del listado: el formulario de corrección pasa a mostrarse antes de la tabla (con desplazamiento suave al inicio al abrirlo) | El modal quedaba al final de la lista y el gestor tenía que desplazarse para verlo; arriba es visible al instante sin perder el contexto de la fila corregida. | Propia (petición del candidato al probar la pantalla) |
| 17/09 | Contrato del plan (§4) actualizado: el request de enmienda incluye `gestorId`, el gestor que corrige | Al implementar la corrección, el modelo del plan §3 exige autor (`AmendedByGestorId` obligatorio) y el request del contrato no lo traía: sin él no habría trazabilidad de quién corrige y no hay autenticación de la que derivarlo. Se corrige el contrato alineándolo con el modelo. | Asistente (hueco hallado al implementar el modelo de §3 y corregido) |
| 18/09 | La regla de idioma se formaliza como regla dura del repositorio (AGENTS.md) con su barrido de idioma obligatorio antes de cada commit de código | El Spanglish recayó dos veces porque la regla vivía solo en la bitácora; ahora está en el archivo de reglas que todo agente lee antes de tocar el repo, con el comando de verificación ejecutable. | Propia (petición del candidato tras las dos recidivas) |
| 18/09 | Identificada la paginación como el siguiente change propuesto, tras la vista del mes | La lista sin paginación decae en UX conforme crece el mes — el seed la deja ya incómoda con ~30 contactos; el criterio de CA-4 es de filtros, así que la paginación merece change propio con su spec (tamaño de página, conteo, efecto en la tira de días). | Propia (observación del candidato al revisar la UX de la pantalla) |

**Herramientas de IA utilizadas:** **OpenCode** como agente de código, con los modelos **GLM 5.3**, **GLM 5.3 Flash** y **DeepSeek V4.1 Flash**. Hasta aquí se usaron en: análisis crítico del extracto y revisión de los entregables, redacción y corrección de los documentos (`01`, `02`, `03`), y decisiones de arquitectura del plan (acceso a datos, contrato de la API). Se usa **OpenSpec** como metodología de trabajo: los cambios, tareas y deltas de especificación se registran en `openspec/` a medida que se resuelven. _Las partes restantes (propuestas de cambios OpenSpec, código, pruebas, README) se declaran al cierre, con la distinción por herramienta._
